const initialState = {
  balances: {
    GBP: 500,
  },
};

const currentUser = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default currentUser;
